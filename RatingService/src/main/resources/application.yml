spring:
  application:
    name: RATING-SERVICE
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: RatingService
  config:
    import: optional:configserver:http://localhost:8085
server:
  port: 8082