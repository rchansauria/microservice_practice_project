server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel_service
    username: root
    password: 1MS08is095@
  config:
    import: optional:configserver:http://localhost:8085

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true